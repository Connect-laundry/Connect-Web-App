# API Configuration
# Local API routes are used by default (Next.js /api routes)
# To use external backend instead, uncomment and set the URL:
# NEXT_PUBLIC_API_BASE_URL=https://connect-full-backend.onrender.com/api/v1
NEXT_PUBLIC_API_BASE_URL=/api
NEXT_PUBLIC_APP_NAME=Connect Laundry Owner
